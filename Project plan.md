# План работы

## Задача 1

- [x] Придумать 2 бизнес задачи и перевести из в DL задачи
- [x] Создать репозиторий на GitHub
- [x] Расписать структуру репозитория и сделать `README.md`, `.gitignore`, `requirements.txt`
- [x] Нарисовать бизнес-схему `данные -> обработка -> модель -> бизнес-решение`
- [x] Расписать структуру репозитория (`data/ notebooks/ src/ models/ docs/`)
- [x] Настроить инструмент логирования экспериментов: MLflow / W&B / DagsHub
- [x] Завести `config.yaml` и единый random_seed для воспроизводимости
- [x] Настроить Dockerfile / окружение для одинакового запуска у всех

## Задача 2

- [x] Найти данные
- [x] Выбрать 1–3 связные категории и целевой объём (сколько товаров / сколько отзывов)
- [x] Спарсить новые данные — парсер `src/parse_catalog.py` (поиск.выдача WB, троттлинг,
      backoff, кэш, возобновляемость). Полный прогон — на своей машине (sandbox-IP в rate-limit)
- [x] Сохранить сырьё отдельно в `data/raw/catalog/` и не трогать. Снапшот — `src/make_snapshot.py`
- [x] Отзывы из `wb-feedbacks` по `nmId` каталога, стратификация по оценке — `src/filter_reviews.py`
- [x] Склеить данные и создать готовый датасет (этап 3 → `data/processed/`)
- [x] Составить data dictionary: поля, типы, что значат — `docs/data_dictionary.md`

## Задача 3

- [x] Предобработка данных: чистка, дедупликация, обработка пропусков и выбросов — `src/prepare_tabular.py`, `src/prepare_text.py`
- [x] **Табличные:** кодирование категориальных, **скейлинг числовых**, новые фичи — `src/preprocess.py`
- [x] **Текстовые:** нормализация, токенизация, обрезка/паддинг длины (max_len по p99) — `src/preprocess.py`, токенизатор ruBERT
- [x] EDA по данным, как текстовым, так и табличным — `notebooks/eda.ipynb`
- [x] Сделать стратифицированные сплиты и сохранить их — `data/processed/`
- [x] Оформить предобработку как воспроизводимый пайплайн — `build_tabular_preprocessor`, fit только на train
- [x] Решить, что делать с дисбалансом классов — веса классов, `data/processed/class_weights.json`
- [x] Сформулировать прикладные инсайты словами — `docs/insights.md` и конец `notebooks/eda.ipynb`

## Задача 4

- [x] Не-нейросетевой baseline для сравнения нужна ли вообще сеть — `notebooks/tabular_mlp.ipynb`
- [x] 3 или более значимо разные конфигурации MLP по глубине / ширине / активациям / регуляризации — `notebooks/tabular_mlp.ipynb`
- [x] Подбор гиперпараметров — описать и обосновать — сетка lr и weight decay, `docs/tabular_mlp.md`
- [x] Кривые обучения, early stopping, фиксация seed
- [x] Каждая конфигурация = отдельный run в трекинге: гиперпараметры + метрики + артефакт модели
- [x] Финальная оценка на test только в конце (val — для подбора) — конец `notebooks/tabular_mlp.ipynb`
- [x] Добавить фичи не из лекций: эмбеддинг бренда (nn.Embedding) в MLP
- [x] Интерпретация важности признаков SHAP / permutation importance — бизнес-инсайт
- [x] Таблица сравнения + явные выводы, как архитектура влияет на качество — `docs/tabular_mlp.md`

## Задача 5

- [x] 3 или более значимо разные архитектуры: TF-IDF/CNN-text -> LSTM/GRU -> fine-tune ruBERT
- [x] Подход не из лекций: fine-tuning предобученного трансформера (ruBERT-tiny2 через Trainer HuggingFace)
- [x] Метрики по эпохам + кривые обучения, early stopping, чекпойнты
- [x] Подбор гиперпараметров (lr, batch, max_len, dropout) — обосновать
- [x] Учесть дисбаланс (веса/focal loss); основная метрика — macro-F1
- [x] Каждая архитектура = отдельный run с артефактом модели + токенизатором (воспроизведение без переобучения)
- [x] Интерпретация: ключевые слова/аспекты на класс — "почему низкий рейтинг" (бизнес-инсайт)
- [x] Таблица сравнения + выводы о сравнительной эффективности архитектур

## Задача 6

- [x] Свести обе модели в единую бизнес-историю + финальная бизнес-схема — `main.ipynb`, связь по `nmId`
- [x] Финальные выводы по всем моделям и данным — конец `main.ipynb` и `README.md`
- [x] Положить train/val-данные в систему логирования и проверить прогон "только из логов" — `src/log_datasets.py`, секция воспроизводимости в `main.ipynb`
- [x] Обновить `main.ipynb` к финальному виду: прогон сверху вниз с чистого ядра, без ошибок
- [x] Финальный `README.md`: описание, данные, структура, как запустить, результаты
- [x] Проверка GitHub: нет данных/секретов/чекпойнтов в гите и понять, что `.gitignore` рабочий
- [x] Проверка коммит-истории — регулярные осмысленные коммиты по этапам
- [ ] Презентация под бизнес-заказчика: схема, читаемые графики, выводы — без кода и скринов ноутбуков
- [ ] Подготовка к защите

## Организационное

- [ ] Логировать эксперименты и делать осмысленные коммиты на всех этапах, а не в конце
- [ ] Таблица кто что делал в README.md
